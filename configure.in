#                                               -*- Autoconf -*-
# Process this file with autoconf to produce a configure script.

AC_INIT(SysteMoC, 0.1, falk@cs.fau.de)
AC_PREREQ(2.57)
ACJF_INIT

AC_CONFIG_SRCDIR([SystemC-Examples.txt])

AC_CONFIG_AUX_DIR([../BuildSystem])
ac_abs_aux_dir=`cd $ac_aux_dir; pwd`
AC_SUBST(ac_abs_aux_dir)
AC_SUBST(ac_aux_dir)

AC_CANONICAL_BUILD
AC_CANONICAL_HOST

dnl Checks for programs.
ACJF_CHECK_CROSSTOOL( CC, gcc)
ACJF_CHECK_CROSSTOOL( CXX, g++)
ACJF_CHECK_CROSSTOOL( AR, ar)
ACJF_CHECK_CROSSTOOL( RANLIB, ranlib)
ACJF_CHECK_CROSSTOOL( NM, nm)
AC_PROG_CXXCPP
AC_PROG_CPP
# Checks for programs.
#AC_PROG_LIBTOOL
#AC_PROG_INSTALL
#AC_PROG_LN_S
#AC_PROG_MAKE_SET

ACJF_ARG_DEBUG([yes], [], [-DNDEBUG])

AM_INIT_AUTOMAKE
AC_CONFIG_HEADER([config.h])

AC_DISABLE_SHARED
AM_PROG_LIBTOOL

dnl check for exe extension
AC_EXEEXT

# Checks for header files.
# AC_FUNC_ALLOCA
# AC_HEADER_STDC
# AC_CHECK_HEADERS([OS.h fcntl.h limits.h mach/mach.h malloc.h stddef.h stdint.h stdlib.h string.h sys/time.h unistd.h])

# Checks for typedefs, structures, and compiler characteristics.

# Checks for library functions.

ACJF_SYSTEMC
ACJF_CHECK_LIB_BOOST

### SystemC-VPC ###
# --enable-vpc option
AC_ARG_ENABLE(
  [vpc],
  [[  --enable-vpc            enable vpc support for SysteMoC [default=yes]]],
  [case "$enableval" in
     yes) enable_vpc=yes ;;
     no)  enable_vpc=no  ;;
     *)   enable_vpc=no  ;;
   esac],
  [enable_vpc=yes # default]
)

if test x"$enable_vpc" = x"yes"; then
  SYSTEMC_VPC_INCLUDE=" \
    -I`pwd`/../Support/include \
    -I`pwd`/../SysteMoC/include/systemoc \
    -I`pwd`/../SystemC-VPC/include/systemcvpc \
    $SYSTEMC_INCLUDE"

  SYSTEMC_VPC_LDFLAGS=" \
    -L`pwd`/../SystemC-VPC \
    -lsystemcvpc \
    $SYSTEMC_LDFLAGS"

  SYSTEMC_VPC_DEPENDENCIES=" \
    `pwd`/../SystemC-VPC/libsystemcvpc.la"

  CPPFLAGS="$CPPFLAGS -DENABLE_SYSTEMC_VPC"
  
  AC_CONFIG_FILES([vpc-test-h261/Makefile])
  subdirs="$subdirs vpc-test-h261"
else
  SYSTEMC_VPC_INCLUDE=""
  SYSTEMC_VPC_LDFLAGS=""
  SYSTEMC_VPC_DEPENDENCIES=""
fi

AC_SUBST([SYSTEMC_VPC_INCLUDE])
AC_SUBST([SYSTEMC_VPC_LDFLAGS])
AC_SUBST([SYSTEMC_VPC_DEPENDENCIES])

### SysteMoC ###
SYSTEMOC_INCLUDE=" \
  -I`pwd`/../Support/include \
  -I`pwd`/../SysteMoC/include/systemoc \
  $SYSTEMC_INCLUDE $BOOST_INCLUDE"
AC_SUBST([SYSTEMOC_INCLUDE])

SYSTEMOC_LDFLAGS=" \
  -L`pwd`/../SysteMoC \
  $SYSTEMC_LDFLAGS $BOOST_LDFLAGS"
AC_SUBST([SYSTEMOC_LDFLAGS])

SYSTEMOC_DEPENDENCIES=" \
  `pwd`/../SysteMoC/libsystemoc.la"
AC_SUBST([SYSTEMOC_DEPENDENCIES])

AC_CONFIG_FILES([Makefile])

if test -d $srcdir/mpeg4-ref; then
  AC_CONFIG_FILES([mpeg4-ref/Makefile])
  subdirs="$subdirs mpeg4-ref"
fi
if test -d $srcdir/idct2d-ref; then
  AC_CONFIG_FILES([idct2d-ref/Makefile])
  subdirs="$subdirs idct2d-ref"
fi
if test -d $srcdir/systemc-csp; then
  AC_CONFIG_FILES([systemc-csp/Makefile])
# subdirs="$subdirs systemc-csp"
fi
if test -d $srcdir/systemc-sdf; then
  AC_CONFIG_FILES([systemc-sdf/Makefile])
  subdirs="$subdirs systemc-sdf"
fi
if test -d $srcdir/systemc-sqr; then
  AC_CONFIG_FILES([systemc-sqr/Makefile])
  subdirs="$subdirs systemc-sqr"
fi
if test -d $srcdir/systemc-fir; then
  AC_CONFIG_FILES([systemc-fir/Makefile])
  subdirs="$subdirs systemc-fir"
fi
if test -d $srcdir/systemc-select; then
  AC_CONFIG_FILES([systemc-select/Makefile])
  subdirs="$subdirs systemc-select"
fi
if test -d $srcdir/systemc-mpeg4; then
  AC_CONFIG_FILES([systemc-mpeg4/Makefile])
  subdirs="$subdirs systemc-mpeg4"
fi
if test -d $srcdir/systemc-ibtest; then
  AC_CONFIG_FILES([systemc-ibtest/Makefile])
  subdirs="$subdirs systemc-ibtest"
fi
if test -d $srcdir/systemc-td-sim-test; then
  AC_CONFIG_FILES([systemc-td-sim-test/Makefile])
# subdirs="$subdirs systemc-td-sim-test"
fi
if test -d $srcdir/systemc-mpeg4-teich-v2; then
  AC_CONFIG_FILES([systemc-mpeg4-teich-v2/Makefile])
# subdirs="$subdirs systemc-mpeg4-teich-v2"
fi

SUBDIRS=$subdirs
AC_SUBST(SUBDIRS)

ACJF_DONE

AC_OUTPUT
