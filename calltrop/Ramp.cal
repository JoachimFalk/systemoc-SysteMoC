/**
 * This actor produces a simple ramp signal.
 *
 * @author LW
 */



actor Ramp [T < Number, Any]
    (T init = 0, T step = 1)
      
    Any In ==> T Out :

    T state := init;

    action In: [trigger] ==> [out]
    var T out = state
    do
        state := state + step;
    end
endactor



