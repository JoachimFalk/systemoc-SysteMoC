apply plugin: 'org.gradle.lifecycle-base'

if (cxxBuild) {
  apply from: 'build-cpp.gradle'
}

//if (parent.name.find("systemoc") == "systemoc") {
//  apply from: 'build-cpp.gradle'
//} else {
//  apply from: 'build-java.gradle'
//}
