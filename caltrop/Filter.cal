actor Filter [T] 
  ([T --> Boolean] filter) 

  T Input ==> T Output:

  // Simply remove the token if the filter function returns flase.
  action [a] ==> [] guard not filter(a) end

  // Let the token through if the filter function returns true.
  action [a] ==> [a] guard filter(a)  end
end
