#ifndef INCLUDED_SMOC_SYNTH_STD_INCLUDES_HPP
#define INCLUDED_SMOC_SYNTH_STD_INCLUDES_HPP

#define SC_USE_STD_STRING
# include <systemc.h>
# include <iostream>

// If custom packet types are used:
// * Provide default constructor
// * bool operator==(const Batz& b);

// * Serializing of tokens
// -> Return expression which is a valid construct expression
// inline std::ostream& operator<<(std::ostream& os, const Batz& b);

// * Tracing:
//   inline void sc_trace( sc_trace_file* tf, const Batz& b, const std::string& s);

// * Interpret functions used by FIFOs to serialize tokens
//   to memory (may be autogenerated in the future)
//     inline void cynw_interpret( const Batz& in, sc_uint<32>& out);
//     inline void cynw_interpret( const sc_uint<32>& in, Batz& out) {

#endif // INCLUDED_SMOC_SYNTH_STD_INCLUDES_HPP
